

virtual  : linkage dynamique, a l execution le compilateur regarde le type reel de l objet
fonction membre virtuelle = une methode 


vitural fonciton = 0 -> methode pure : on ne peut pas l implementer, classe abstraite
on ne peut pas l utiliser dans la classe mere mais on peut l utiliser dans els classes filles si la fonciton est implementée

interface : uniquement des meyhodes pures et aucun attribut
c est une maniere de definir un comportement pour les objets
